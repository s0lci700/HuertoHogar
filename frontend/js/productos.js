window.HHProductos = (function () {
  // TODO: hacer fetch de ../database/productos.json y devolver el JSON parseado (cachear si quieres)
  async function cargarCatalogo() {}

  // TODO: usar cargarCatalogo() y buscar el producto cuyo "codigo" coincida
  async function buscarPorCodigo(codigo) {}

  return { cargarCatalogo, buscarPorCodigo };
})();
